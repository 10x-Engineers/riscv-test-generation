# Generated by trap_cases.py -- do not edit by hand.

# exceptions: 24
# interrupts: 14
# priority: MEI > MSI > MTI > SEI > SSI > STI > LCOFI
# cases: 16
# exclusions: 8
# config: rv64.json

[[case]]
id       = "exc_e_breakpoint"
kind     = "exception"
priv     = "M"
handler  = "M"
expect   = "trap:3"
opcode   = 0x100073
medeleg  = 0x0
why      = "ebreak -> cause 3, taken in Machine mode"

[[case]]
id       = "exc_e_m_envcall"
kind     = "exception"
priv     = "M"
handler  = "M"
expect   = "trap:11"
opcode   = 0x73
medeleg  = 0x0
why      = "ecall from Machine -> cause 11, taken in Machine mode"

[[case]]
id       = "exc_e_s_envcall"
kind     = "exception"
priv     = "S"
handler  = "M"
expect   = "trap:9"
opcode   = 0x73
medeleg  = 0x0
why      = "ecall from Supervisor -> cause 9, taken in Machine mode"

[[case]]
id       = "exc_e_u_envcall"
kind     = "exception"
priv     = "U"
handler  = "M"
expect   = "trap:8"
opcode   = 0x73
medeleg  = 0x0
why      = "ecall from User -> cause 8, taken in Machine mode"

[[case]]
id       = "deleg_e_breakpoint_to_s"
kind     = "exception"
priv     = "S"
handler  = "S"
expect   = "trap:3"
opcode   = 0x100073
medeleg  = 0x8
why      = "medeleg[3] set and the hart is in Supervisor: exception_delegatee returns Supervisor, so scause holds the cause"

[[case]]
id       = "deleg_e_breakpoint_from_m"
kind     = "exception"
priv     = "M"
handler  = "M"
expect   = "trap:3"
opcode   = 0x100073
medeleg  = 0x8
why      = "medeleg[3] set but the hart is in Machine: delegation cannot lower the privilege, so the trap stays in Machine mode"

[[case]]
id       = "prio_sei_over_ssi"
kind     = "interrupt"
priv     = "M"
handler  = "M"
expect   = "interrupt:9"
medeleg  = 0x0
mideleg  = 0x0
mip      = 0x202
mie      = 0x202
why      = "SEI and SSI pending together; findPendingInterrupt checks SEI first, so I_S_External (cause 9) must be the one taken"

[[case]]
id       = "prio_ssi_over_sti"
kind     = "interrupt"
priv     = "M"
handler  = "M"
expect   = "interrupt:1"
medeleg  = 0x0
mideleg  = 0x0
mip      = 0x22
mie      = 0x22
why      = "SSI and STI pending together; findPendingInterrupt checks SSI first, so I_S_Software (cause 1) must be the one taken"

[[case]]
id       = "prio_sti_over_lcofi"
kind     = "interrupt"
priv     = "M"
handler  = "M"
expect   = "interrupt:5"
medeleg  = 0x0
mideleg  = 0x0
mip      = 0x2020
mie      = 0x2020
why      = "STI and LCOFI pending together; findPendingInterrupt checks STI first, so I_S_Timer (cause 5) must be the one taken"

[[case]]
id       = "deleg_sei_to_s"
kind     = "interrupt"
priv     = "S"
handler  = "S"
expect   = "interrupt:9"
medeleg  = 0x0
mideleg  = 0x200
mip      = 0x200
mie      = 0x200
why      = "mideleg[SEI] is writable, so I_S_External is delivered to Supervisor and the cause appears in scause, not mcause"

[[case]]
id       = "deleg_ssi_to_s"
kind     = "interrupt"
priv     = "S"
handler  = "S"
expect   = "interrupt:1"
medeleg  = 0x0
mideleg  = 0x2
mip      = 0x2
mie      = 0x2
why      = "mideleg[SSI] is writable, so I_S_Software is delivered to Supervisor and the cause appears in scause, not mcause"

[[case]]
id       = "deleg_sti_to_s"
kind     = "interrupt"
priv     = "S"
handler  = "S"
expect   = "interrupt:5"
medeleg  = 0x0
mideleg  = 0x20
mip      = 0x20
mie      = 0x20
why      = "mideleg[STI] is writable, so I_S_Timer is delivered to Supervisor and the cause appears in scause, not mcause"

[[case]]
id       = "deleg_lcofi_to_s"
kind     = "interrupt"
priv     = "S"
handler  = "S"
expect   = "interrupt:13"
medeleg  = 0x0
mideleg  = 0x2000
mip      = 0x2000
mie      = 0x2000
why      = "mideleg[LCOFI] is writable, so I_COF is delivered to Supervisor and the cause appears in scause, not mcause"

[[case]]
id       = "xtval_e_m_envcall"
kind     = "exception"
priv     = "M"
handler  = "M"
expect   = "trap:11"
opcode   = 0x73
medeleg  = 0x0
check_xtval = false
why      = "xtval_exception_value returns false for E_M_EnvCall, so mtval must read zero"

[[case]]
id       = "xtval_e_s_envcall"
kind     = "exception"
priv     = "S"
handler  = "M"
expect   = "trap:9"
opcode   = 0x73
medeleg  = 0x0
check_xtval = false
why      = "xtval_exception_value returns false for E_S_EnvCall, so mtval must read zero"

[[case]]
id       = "xtval_e_u_envcall"
kind     = "exception"
priv     = "U"
handler  = "M"
expect   = "trap:8"
opcode   = 0x73
medeleg  = 0x0
check_xtval = false
why      = "xtval_exception_value returns false for E_U_EnvCall, so mtval must read zero"
